{
  "name": "@blast-radius/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core data models, validation, and shared utilities for Blast Radius Visualizer",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
